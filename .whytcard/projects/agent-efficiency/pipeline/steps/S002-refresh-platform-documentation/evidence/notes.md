# Notes

- The documentation now treats official Cursor and Claude Code docs as the primary model and keeps community reports in caveat-only sections.
- The repo docs now use `subagent` as the main delegation term and define plugin wrapper modes as repo conventions rather than official host modes.
- The exhaustive-reading contract is now explicit across the core docs, command wrappers, rules, and key skills to prevent false claims of full understanding after partial reading.
